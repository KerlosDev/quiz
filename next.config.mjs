
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com','i.postimg.cc'], // Add the domains you want to allow
    },
};


export default nextConfig;
